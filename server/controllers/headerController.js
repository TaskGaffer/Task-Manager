// const db = require('../models/model)

const headerController = {};

// get headers
headerController.getHeader = (req, res, next) => {};

// new header
headerController.newHeader = (req, res, next) => {};

module.exports = headerController;
